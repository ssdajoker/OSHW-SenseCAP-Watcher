# Architecture

The timer app runs on the SenseCAP Watcher using the ESP-IDF framework. It creates a FreeRTOS task that increments a counter every second, acting as a simple stopwatch or pomodoro timer.
